// ============================================================================
// AutoPitch.h —— 自动音高模块 (纯桩)
// ----------------------------------------------------------------------------
// ★ 算法体被 NDEBUG 裁剪, 不可恢复 (逆向报告"不可恢复清单")。
// 接口按数据流位置推断: I2S/ADC → 本模块 → 输出/MIDI。实机需自行实现
// (FFT 频谱 → 基频检测 → 音高修正)。
// ============================================================================
#pragma once

#include "Common.h"

class CAutoPitch {
public:
    static void Init();                       // ADC/I2S 链路配置 (EADC1VoltageRange TODO)
    // 处理一帧输入样本 → 修正后样本 (骨架: 直通)
    static void Process(const float* in, float* out, int n);
};
