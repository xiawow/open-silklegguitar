// ============================================================================
// PluckDetector.h —— 拨弦检测 (纯桩)
// ----------------------------------------------------------------------------
// ★ 算法体被 NDEBUG 裁剪, 不可恢复。按数据流位置推断: 输入流 → 检出拨弦
// 事件 → 触发音符/MIDI。实机需自行实现 (常见做法: 能量包络突变检测)。
// ============================================================================
#pragma once

#include "Common.h"

class CPluckDetector {
public:
    static void Init();
    // 喂一帧样本; 检出拨弦返回 true (骨架: 恒 false)
    static bool Feed(const float* samples, int n);
};
